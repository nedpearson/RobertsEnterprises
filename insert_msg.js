const { createClient } = require('@supabase/supabase-js'); require('dotenv').config({path: 'apps/marketing/.env.local'}); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_SERVICE_ROLE_KEY || process.env.VITE_SUPABASE_ANON_KEY); async function run() { const { data } = await supabase.from('messages').insert({ customer: 'c_test', body: 'Yes I can make it!', direction: 'inbound', channel: 'sms', sent_at: new Date().toISOString() }).select(); console.log(data); } run();