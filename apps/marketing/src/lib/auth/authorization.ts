/**
 * Canonical Authorization & RBAC Model for VowOS Platform
 * 
 * Re-exports the authoritative authorization engine from the core lib/auth package.
 */
export * from '../../../worker/src/lib/auth/authorization';
